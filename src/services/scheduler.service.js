const cron = require('node-cron');

class SchedulerService {
    /**
     * Start cron job to check whether invoice's paymentDueDatetime is past expiration and change its status
     * @todo Will do :p
     * @static
     */
    static start() {
        //
    }
}
